document.querySelectorAll('.brand-image').forEach((image)=>{image.addEventListener('error',()=>{image.closest('[data-logo]')?.classList.add('logo-error')},{once:true})});document.querySelectorAll('a[href^="#"]').forEach((link)=>{link.addEventListener('click',(event)=>{const target=document.querySelector(link.getAttribute('href'));if(target){event.preventDefault();target.scrollIntoView({behavior:'smooth',block:'start'})}})});